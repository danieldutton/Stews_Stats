import XCTest
@testable import Stew_s_Stats

class StatisticsFactoryTests: XCTestCase {

    func test_foo() {
        /*
        let sut = StatisticsFactory()
        
        let expected = AnnualActivityStatisticsSeedData().getSeedData()
        let actual = sut.getStatistics(key: .daily)
        
        XCTAssertEqual(expected, actual)
        */
    }
}
